[package]
name = "comparer"
version = "0.1.11"
edition = "2021"
description = "A library to track changes in a HashMap over several iterations"
license = "MIT"
readme = "README.md"
repository = "https://github.com/ElmerByte/rust-comparer"
[dependencies]
